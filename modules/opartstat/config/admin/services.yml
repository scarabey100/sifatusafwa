services:
  _defaults:
    public: true

  ##############
  # Your Module

  opartstat.module:
    class: opartstat
    factory: ['Module', 'getInstanceByName']
    arguments:
      - 'opartstat'

  opartstat.context:
    class: Context
    factory: ['Context', 'getContext']

  #####################
  # PrestaShop Account
  opartstat.ps_accounts_installer:
    class: 'PrestaShop\PsAccountsInstaller\Installer\Installer'
    arguments:
      - '5.0'

  opartstat.ps_accounts_facade:
    class: 'PrestaShop\PsAccountsInstaller\Installer\Facade\PsAccounts'
    arguments:
      - '@opartstat.ps_accounts_installer'

#####################
  # PrestaShop Billing

  opartstat.ps_billings_context_wrapper:
    class: 'PrestaShopCorp\Billing\Wrappers\BillingContextWrapper'
    arguments:
    - '@opartstat.ps_accounts_facade'
    - '@opartstat.context'
    - false # if true, enables the sandbox mode, if false or empty, disables it

  opartstat.ps_billings_facade:
    class: 'PrestaShopCorp\Billing\Presenter\BillingPresenter'
    public: true
    arguments:
    - '@opartstat.ps_billings_context_wrapper'
    - '@opartstat.module'

  # Remove this if you do not need BillingService
  opartstat.ps_billings_service:
    class: 'PrestaShopCorp\Billing\Services\BillingService'
    public: true
    arguments:
    - '@opartstat.ps_billings_context_wrapper'
    - '@opartstat.module'


